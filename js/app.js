document.addEventListener('DOMContentLoaded', () => {
    App.init();

    initLoader();
    initCursor();
    initProgressBar();
    initParticles();
    initFloating();
    initLetter();
    initEffects();
});
