/* ============================================================
   APP - Inicializador principal
   ============================================================ */
document.addEventListener('DOMContentLoaded', () => {
    App.init();

    initLoader();
    initCursor();
    initProgressBar();
    initCounter();
    initCarousel();
    initParallax();
    initParticles();
    initFloating();
    initLetter();
    initAudio();
    initEffects();
});
