/* ============================================================
   APP.JS - Inicializador principal
   ============================================================ */
document.addEventListener('DOMContentLoaded', () => {
    initLoader();
    initCursor();
    initProgressBar();
    initCounter();
    initCarousel();
    initParallax();
    initParticles();
    initFloating();
    initLetter();
    initAudio();
    initEffects();
});
